type Query {
  allAuthors: [Author]
  author(id: ID!): Author!
}

type Author {
  id: ID!
  firstName: String
  lastName: String
}

type Mutation {
  createAuthor(author: AuthorInput!): Author
  deleteAuthor(id: ID!): String
}

input AuthorInput {
  firstName: String!
  lastName: String!
}
