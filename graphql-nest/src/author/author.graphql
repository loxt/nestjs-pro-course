type Query {
  allAuthors: [Author]
  author(id: ID!): Author!
}

type Author {
  id: ID!
  firstName: String
  lastName: String
}

type Mutation {
  createAuthor(author: AuthorInput!): Author
}

input AuthorInput {
  firstName: String!
  lastName: String!
}
